import loginWhitGoogle from "../Firebase/func";
// import {LogIn } from "../LogIn";

it('loginWhitGoogle', () => {
  // inicializacion
  // estimulo
  // observamos el comportamiento 
  loginWhitGoogle()
})

describe('login', () => {
  it('si ejecutamos la funcion login no debe fallar').toBe()
})